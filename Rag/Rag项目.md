# 1. 项目简介

A I 大模型  
Part : RAG 实战项目  
RAG 与智能体开发

## RAG 回顾

RAG 即检索、增强和生成，其主要分为 2 条线：

- **离线处理**：向私有知识库（向量存储）源源不断添加私有知识文档。
  - 向知识库添加来自未来的知识文档（基于模型训练完成时间）
  - 向模型添加私有知识文档
  - 给出模型参考资料，规避模型幻觉（一本正经的胡说八道）

- **在线处理**：用户提问会先基于私有知识库做检索，获取参考资料，同步组装新提示词询问大模型获取结果。

---

# 2. 项目需求和思路

本次项目以"某东商品衣服"为例，以衣服属性构建本地知识。使用者可以**自由更新**本地知识，用户问题的答案也是**基于本地知识**生成的。

### 离线流程

本地知识文件 → 加载和读取 → 文本切分 → 向量数据库

### 在线流程

Query → 向量化 → 向量匹配 → Prompt 工程 → 提交 LLM 生成答案

---

# 3. 项目主要实现代码

项目主要会实现如下代码：

- `app_file_upload.py` —— 知识库更新主程序（streamlit）
- `app_qa.py` —— 项目主程序（streamlit），启动对话 WEB 页面
- `config_data.py` —— 配置文件
- `file_history_store.py` —— 长期会话记忆存储服务
- `knowledge_base.py` —— 知识库更新服务
- `rag.py` —— RAG 核心服务
- `vector_stores.py` —— 向量存储服务

---

# 4. 项目需求和思路（重复内容）

本次项目以"某东商品衣服"为例，以衣服属性构建本地知识。使用者可以**自由更新**本地知识，用户问题的答案也是**基于本地知识**生成的。

### 离线流程

本地知识文件 → 加载和读取 → 文本切分 → 向量数据库

### 在线流程

Query → 向量化 → 向量匹配 → Prompt 工程 → 提交 LLM 生成答案

---

# 5. 项目主要实现代码（重复内容）

项目主要会实现如下代码：

- `app_file_upload.py` —— 知识库更新主程序（streamlit）
- `app_qa.py` —— 项目主程序（streamlit），启动对话 WEB 页面
- `config_data.py` —— 配置文件
- `file_history_store.py` —— 长期会话记忆存储服务
- `knowledge_base.py` —— 知识库更新服务
- `rag.py` —— RAG 核心服务
- `vector_stores.py` —— 向量存储服务

---

# 6. 离线流程涉及的模块

离线流程：

- `app_file_upload.py`
- `config_data.py`
- `knowledge_base.py`

---

# 7. 项目需求和思路（再次重复）

本次项目以"某东商品衣服"为例，以衣服属性构建本地知识。使用者可以**自由更新**本地知识，用户问题的答案也是**基于本地知识**生成的。

### 离线流程

本地知识文件 → 加载和读取 → 文本切分 → 向量数据库

### 在线流程

Query → 向量化 → 向量匹配 → Prompt 工程 → 提交 LLM 生成答案

---

# 8. 在线流程

在线流程：

（此处原文仅有“在线流程：”字样，无更多具体描述，按原内容保留）
